name: Trivy Scan

on:
  push:
  pull_request:

jobs:
  scan:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Run Trivy Scan
      uses: aquasecurity/trivy-action@v0.35.0
      with:
        image-ref: 'vulnerabilities/web-dvwa:latest'
        severity: 'HIGH,CRITICAL'
        exit-code: '1'